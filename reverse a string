<?php
function stringReverse($str)
{
 //return strrev($str); 
 $reversed = "";
 for($i=strlen($str)-1;$i >= 0;$i--)
 {
   $reversed .= $str[$i];
 }
 return $reversed;
}

$str = "i am in germany";
echo stringReverse($str);
?>
